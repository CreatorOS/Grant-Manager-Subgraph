{
  "name": "grant-manager-subgraph-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dhairya-creatoros/grant-manager-subgraph",
    "create-local": "graph create --node http://localhost:8020/ dhairya-creatoros/grant-manager-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ dhairya-creatoros/grant-manager-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dhairya-creatoros/grant-manager-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
